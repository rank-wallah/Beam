{
  "name": "zipline",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end encrypted, peer-to-peer file transfer over the internet. No uploads. No accounts.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @zipline/server --filter @zipline/client dev",
    "dev:server": "pnpm --filter @zipline/server dev",
    "dev:client": "pnpm --filter @zipline/client dev",
    "build": "pnpm --filter @zipline/shared build && pnpm --filter @zipline/server build && pnpm --filter @zipline/client build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
